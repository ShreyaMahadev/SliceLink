<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Modern Node.js URL Shortener">
  <title>SliceLink</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    body {
      background: linear-gradient(135deg, #f9f9f9, #e0f7fa);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    nav {
      background-color: #007bff;
    }
    nav .navbar-brand {
      color: #fff;
      font-weight: bold;
    }
    .main-header {
      text-align: center;
      padding: 4rem 1rem;
    }
    .main-header h1 {
      font-size: 2.5rem;
      color: #007bff;
      font-weight: 600;
    }
    .shortener {
      max-width: 800px;
      margin: 2rem auto;
    }
    .shortener input[type="text"] {
      width: 100%;
      padding: 1rem;
      border-radius: 0.5rem;
      border: 1px solid #ccc;
      margin-bottom: 1rem;
    }
    .shortener button {
      width: 100%;
      padding: 1rem;
      background-color: #007bff;
      border: none;
      color: white;
      font-weight: bold;
      border-radius: 0.5rem;
    }
    .features, .stats {
      margin: 3rem 0;
      text-align: center;
    }
    .feature-box i {
      font-size: 2rem;
      color: #007bff;
      margin-bottom: 1rem;
    }
    .feature-box {
      padding: 2rem;
      background: #fff;
      border-radius: 1rem;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      margin: 1rem;
    }
    .liveNumber {
      font-size: 3rem;
      font-weight: bold;
      color: #007bff;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg">
    <div class="container">
      <a class="navbar-brand" href="#">SliceLink</a>
    </div>
  </nav>

  <div class="main-header">
    <h1>Simplify Your Links. Share Effortlessly.</h1>
  </div>

  <div class="container shortener">
    <input type="text" id="shortenLink" class="form-control" placeholder="Paste your long URL here...">
    <input type="text" id="shortenLinkCopy" class="form-control" readonly placeholder="Your shortened URL will appear here">
    <button id="shortenLinkButton">Shorten URL</button>
  </div>

  <script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
  <script>
    $('#shortenLinkButton').click(function () {
      const url = $('#shortenLink').val();
      $.post('/short', { url }, function (data) {
        $('#shortenLinkCopy').val(data);
      }).fail(function () {
        alert('Invalid URL or error occurred.');
      });
    });
  </script>
</body>
</html>
